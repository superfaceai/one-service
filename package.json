{
  "name": "one-service",
  "version": "0.0.0",
  "description": "HTTP Service able to execute Superface capabilities",
  "main": "dist/index.js",
  "repository": "https://github.com/superfaceai/one-service",
  "author": "Superface Team <hello@superface.ai>",
  "license": "MIT",
  "files": [
    "bin/",
    "dist/"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json --outDir dist",
    "build:watch": "yarn build --watch",
    "test": "jest",
    "start": "./bin/server",
    "start:dev": "concurrently \"yarn build:watch\" \"nodemon -w dist dist/index.js\""
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^16.10.3",
    "concurrently": "^6.3.0",
    "jest": "^27.2.5",
    "nodemon": "^2.0.13",
    "typescript": "^4.4.3"
  }
}
